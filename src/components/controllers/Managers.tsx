import AntConfig from "../AntConfig";
import ModalManager from "./ModalManager";

const Managers = () => {
    return (
        <AntConfig>
            <ModalManager />
        </AntConfig>
    );
};

export default Managers;
