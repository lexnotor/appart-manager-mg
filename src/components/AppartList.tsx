import { CustomTable } from "./CustomTable";
const AppartList = () => {
    return (
        <div>
            <CustomTable columns={[]} dataSource={[]} />
        </div>
    );
};

export default AppartList;
